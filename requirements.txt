# ── Core Vision & OCR ────────────────────────────────────────────────────────
opencv-python>=4.8.0
numpy>=1.24.0
scikit-image>=0.21.0
easyocr>=1.7.0

# ── Web Framework ─────────────────────────────────────────────────────────────
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9    # Required for FastAPI file upload (UploadFile)
sqlalchemy>=2.0.0

# ── LangChain & LLM ───────────────────────────────────────────────────────────
langchain>=0.2.0
langchain-community>=0.2.0
langchain-text-splitters>=0.2.0
langchain-openai>=0.1.0
langchain-ollama>=0.1.0    # Replaces deprecated langchain_community.llms.Ollama

# ── Vector Database ───────────────────────────────────────────────────────────
chromadb>=0.5.0

# ── Embeddings ────────────────────────────────────────────────────────────────
sentence-transformers>=2.7.0  # Required for HuggingFaceEmbeddings

# ── Testing ───────────────────────────────────────────────────────────────────
pytest>=7.4.0
httpx>=0.27.0              # Required for FastAPI TestClient (async)

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0
langchain-experimental>=0.0.60
